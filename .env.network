# Network configuration for external access
# Development host IP - will change in production
HOST_IP=<INSERTIP>

# Frontend will connect to this backend URL
REACT_APP_API_URL=http://<INSERTIP>:8000

# Backend will allow CORS from these origins
CORS_ORIGINS=http://localhost:3000,http:/<INSERTIP>:3000