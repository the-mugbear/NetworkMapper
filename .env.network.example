# Network configuration for external access
# Copy this file to .env.network and update with your server's IP address

# Replace with your server's actual IP address
HOST_IP=YOUR_SERVER_IP

# Frontend will connect to this backend URL
REACT_APP_API_URL=http://YOUR_SERVER_IP:8000

# Backend will allow CORS from these origins
CORS_ORIGINS=http://localhost:3000,http://YOUR_SERVER_IP:3000

# Example configurations:
# For local network: HOST_IP=192.168.1.100
# For cloud server: HOST_IP=10.0.1.50
# For public server: HOST_IP=203.0.113.10